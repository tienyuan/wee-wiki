%h1 
  = @wiki.title
%p 
  = @wiki.description
%p
  - if current_user
    = link_to "Add Page", new_wiki_page_path(@wiki)
    = link_to "Edit Wiki", edit_wiki_path(@wiki)

%h4 Wiki Pages
%ul
  - @pages.each do |page|
    %li
      = link_to page.title, wiki_page_path(@wiki, page)

%h4 Current Collaborators
%p
  %b Add 
  - render partial: 'collaborators', locals: {wiki: @wiki}

%p username x to remove