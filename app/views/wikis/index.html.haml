%h1 Wiki List
%div.row
  %div.col-md-8
    - if current_user
      = link_to "Create Wiki", new_wiki_path
%div.row
  %div.col-md-8
    - @wikis.each do |wiki|
      %h4
        = link_to wiki.title, wiki
      %em
        = wiki.description
